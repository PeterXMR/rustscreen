# Rust
/target
**/target
Cargo.lock.bak
*.rs.bk

# Android
/android/.gradle
/android/build
/android/app/build
/android/local.properties
/android/.idea
**/jniLibs/*/*.so
*.apk
*.aab
*.keystore
# Signing credentials (passwords to the keystore). build.gradle.kts reads
# android/keystore.properties; never commit it. The .example template is fine.
keystore.properties

# macOS
.DS_Store
*.app
*.dmg

# Spike artifacts
*.h264
*.hevc
*.mp4

# Editor / IDE
.vscode
.idea
*.swp

# Claude Code local state (personal settings + agent worktrees — never committed)
.claude/
